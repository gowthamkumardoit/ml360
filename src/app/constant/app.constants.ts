export const URL = 'http://127.0.0.1';
export const PORT = '5000';

export const TOOLTIPS = {
    head: 'Return the first `5` rows. This function returns the first `5` rows for the object based on position. It is useful for quickly testing if your object has the right type of data in it',
    summary: `Generate descriptive statistics that summarize the central tendency, dispersion and shape of a dataset(s) distribution, excluding NAN values.  
                Analyzes both numeric and object series, as well as DataFrame column sets of mixed data types. The output
                will vary depending on what is provided. Refer to the notes below for more detail.`,
    missingValues: 'Returns count and perecentage of missing values in each column of the dataset.',
    skew: `SKEW: Return unbiased skew over requested axis
    Normalized by N-1. --- KURTOSIS: Return unbiased kurtosis over requested axis using Fisher's definition of
    kurtosis (kurtosis of normal == 0.0). Normalized by N-1.`

}

